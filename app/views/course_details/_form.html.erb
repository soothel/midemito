<%= form_with(model: course_detail) do |form| %>
  <% if course_detail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(course_detail.errors.count, "error") %> prohibited this course_detail from being saved:</h2>

      <ul>
        <% course_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :course_id, style: "display: block" %>
    <%= form.number_field :course_id %>
  </div>

  <div>
    <%= form.label :deleted, style: "display: block" %>
    <%= form.number_field :deleted %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
